type Group {
  id: Int
  name: String
  hosts: [Host]
}

extend type Query {
  groups(grp_name: String): [Group]
}

extend type Mutation {
  addGroup(name: String!): Group
  deleteGroup(name: String!): Boolean!
}
